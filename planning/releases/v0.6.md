# Release V0.6 — Inference

**Status:** Complete (untagged) — all scoped features merged; cut `v0.6` when release sign-off accepted

## Goals

Real GGUF inference via llama.cpp.

## Features

- [x] llama.cpp CPU adapter — load/unload (009a)
- [x] mmap GGUF loading (009a)
- [x] Single-prompt CPU Generate (009b)
- [x] Runtime ↔ `LlamaProvider` wiring (009c)
- [x] GPU offload (CUDA) — 009d; `-tags cuda`, `NGLayers`; matrix **Partial** until manual GPU checklist

## Completed Tasks

- 009a — llama.cpp CPU load/unload (2026-06-08, `f67c491`)
- 009b — CPU Generate (2026-06-09, `8cdd9b1`)
- 009c — runtime/loader integration (2026-06-09, `ee186c1`, merge `f20475f`)
- 009d — CUDA GPU offload (2026-06-10, merge `ab06c60`, PR #9)

## Deferred Features

- Metal, ROCm, Vulkan backends
- CI GPU integration / compile-only CUDA job
- SaveContext / RestoreContext (task 011)

## Known Issues

- CUDA runtime proof requires manual GPU machine (`planning/decisions/009d-manual-gpu-checklist.md`)
- CUDA runtime DLLs/so not bundled by TinyBrain

## Demo

Load a small GGUF via `ModelRuntime` (`NewIntegratedModelRuntime` + `RegistryResolver`), call `Generate` with one prompt, receive complete response. CUDA: build with `-tags cuda`, set `LlamaConfig.NGLayers` (e.g. `-1`) at composition root.

## Lessons

- Tokenize with explicit `text_len` and `add_special=false` at llama.cpp pin b9553 (SmolLM vocab)
- `llama_memory_clear` replaces absent `llama_kv_cache_clear` at pin
- Each CGO backend file needs its own `llama.h` preamble (009d CGO split)

---
**Layer:** planning
