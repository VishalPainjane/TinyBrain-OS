# Release V0.7 — Scheduler

**Status:** Shipped (tag `v0.7` @ `a0f90a7`, merge PR #12)

## Goals

MLFQ scheduler with preemption.

## Features

- Q0–Q3 queues with per-level token quanta (32/64/128/256)
- Preemption when higher-priority work arrives
- Boost/aging every 500 tokens or 30s wall clock
- Swap idle heuristic (`ShouldSwap`, 10s threshold — policy only)
- `QueueDepths()` for telemetry

## Completed Tasks

- 010-scheduler-mlfq

## Demo

`TestMLFQScheduler_Preemption` — low-priority runner preempted when high-priority process is scheduled.

## Deferred at v0.7 ship

- Runtime load orchestration in `Schedule`

## Post-v0.7 on main (not in tag)

- 011 KV manager, 012 swap manager, 013 brain-top prototype (Month 3 memory foundations)

## Lessons

- FIFO skeleton behind `Queue` interface enabled MLFQ without contract breakage

---
**Layer:** planning
