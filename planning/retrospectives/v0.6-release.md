# Retrospective — V0.6 Inference Release

**Date:** 2026-06-11  
**Tag:** `v0.6`

## What shipped

- llama.cpp CPU load/unload/generate (009a–009b)
- Runtime ↔ `LlamaProvider` integration (009c)
- CUDA GPU offload adapter (009d); manual GPU checklist still open

## What went well

- CGO split by backend kept CPU CI stable while adding CUDA
- Shared `ModelResolver` avoided duplicate registry lookups
- Integration CI with checksum-verified SmolLM2 caught real regressions

## Problems

- README and `progress.md` drifted after 009d merged
- No composition root (`cmd/`) — users still need test harness to run inference
- CUDA matrix remains Partial without GPU runner or signed checklist

## Decisions

- Tag `v0.6` without blocking on manual GPU sign-off
- Next sprint: `cmd/tinybrain` CLI as product entry point

## Post-release (2026-06-11)

- `cmd/tinybrain` CLI shipped (`stab-003`) — addresses “no composition root” problem above
- Repository hygiene + testing policy added (solo-dev; not community OSS)
- Git tag `v0.6` still pending on release-docs commit

## Lessons

- Ship release docs in the same PR as the final task when possible
- Product-visible work should follow kernel milestones quickly

---
**Layer:** planning
