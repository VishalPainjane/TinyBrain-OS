# V0.8 — Agents

## Goal

Plugin agent execution — end-to-end task pipeline with two sample agents via events.

## Features

- [ ] Agent plugin contract implementation
- [ ] Sample fleet: two agents from registry config (e.g., planner → coder workflow)
- [ ] Structured JSON IPC between agents via event bus
- [ ] Tool request routing to tool layer (stub acceptable)

## Acceptance Criteria

- [ ] Task flows: submit → route → schedule → execute → complete
- [ ] No hardcoded agent types in core
- [ ] Peak VRAM < 3.6 GB on Standard profile for 2-agent demo

## Out Of Scope

- Browser/reasoner agents, RAG, autonomous loops, cloud fallback

## Demo

Submit task; two sample agents execute sequentially via events; structured JSON output returned.

## Dependencies

- v0.7 Scheduler, v0.6 Inference, v0.2 Registry

## Tasks

- Future agent plugin tasks (post-010)

---
**Layer:** spec
**Related:** [../../planning/releases/v0.8.md](../../planning/releases/v0.8.md)
